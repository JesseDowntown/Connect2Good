= form_for @offer do |f|
  - if signed_in?
    - donor_id = current_user.id
  - else
    - donor_id = ""
  = f.label :description
  = f.text_field :description
  = f.label :image
  = f.text_field :image
  = f.submit